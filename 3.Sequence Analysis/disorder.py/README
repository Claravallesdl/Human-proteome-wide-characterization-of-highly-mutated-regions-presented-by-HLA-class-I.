## Overview

This script is designed to analyze and compare coordinates of disorder regions and hotspot regions within genes. It processes input data to extract relevant coordinate information and then compares these coordinates to identify overlaps. The script uses regular expressions for coordinate extraction and pandas for data handling.

## Requirements

- Python 3.x
- `numpy`
- `pandas`
- `statsmodels`
